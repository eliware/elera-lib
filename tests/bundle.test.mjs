import { validateBundle, bundleExpired, bundleNeedsRefresh } from '../src/bundle.mjs';
const valid = { expiresAt: '2030-01-01T00:00:00Z', routes: { primary: [{ host: 'a', port: 3306 }] } };
test('validates bundles and expiry helpers', () => { expect(validateBundle(valid)).toBe(valid); expect(bundleExpired(valid, 0)).toBe(false); expect(bundleNeedsRefresh(valid, 0)).toBe(false); expect(bundleExpired({ ...valid, expiresAt: '2000-01-01T00:00:00Z' }, Date.parse('2030-01-01T00:00:00Z'))).toBe(true); expect(bundleNeedsRefresh({ ...valid, refreshAfter: '2000-01-01T00:00:00Z' }, Date.parse('2030-01-01T00:00:00Z'))).toBe(true); });
test('rejects malformed bundles and nodes', () => { expect(() => validateBundle()).toThrow(); expect(() => validateBundle({})).toThrow(); expect(() => validateBundle({ ...valid, routes: { primary: 'bad' } })).toThrow(); expect(() => validateBundle({ ...valid, routes: { primary: [{ host: '', port: 3306 }] } })).toThrow(); expect(() => validateBundle({ ...valid, routes: { primary: [{ host: 'a', port: 0 }] } })).toThrow(); expect(() => validateBundle({ ...valid, routes: { primary: [{ host: 'a', port: 3306, weight: -1 }] } })).toThrow(); });
